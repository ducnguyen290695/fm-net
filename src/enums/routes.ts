export enum AppRouteE {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  DASHBOARD = '/dashboard',
  PROFILE = '/profile',
  SETTINGS = '/settings',
  USERS = '/admin/users',
  USER_DETAIL = '/admin/users/:id',
  NOT_FOUND = '/404',
}
